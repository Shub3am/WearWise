{
  "name": "@wearwise/ingest",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "go generate ./...",
    "sqlc": "docker run --rm --user \"$(id -u):$(id -g)\" --volume \"$(git rev-parse --show-toplevel)\":/src --workdir /src/apps/ingest sqlc/sqlc:1.31.1 generate",
    "typecheck": "go vet ./...",
    "test": "go test -count=1 ./..."
  },
  "devDependencies": {
    "@wearwise/metrics-catalog": "workspace:*"
  }
}
